---
# title: JSON
author: Gérard LE REST
date: 2025-12-26 11:30:00 +0800
categories: [Python, Cours]
tags: [Json, Dictionnaire]
---

# 📄 JSON – Résumé pratique (usage courant)

## 🔹 Qu’est-ce que JSON ?

**JSON (JavaScript Object Notation)** est un format texte simple utilisé principalement pour :

- les fichiers de configuration
- le paramétrage d’applications
- l’échange de données simples

➡️ Lisible par l’humain  
➡️ Facilement exploitable par Python

---

## 🔹 Structure de base

Un fichier JSON représente le plus souvent **un objet** (équivalent d’un dictionnaire Python).

```json
{
  "Organisme": "lycee",
  "BaseDeDonnees": "eleves.db",
  "DossierPhotos": "photos/lycee"
}
```

---

## 🔹 Types utilisés (cas courants)

| JSON           | Python |
| -------------- | ------ |
| `{}`           | `dict` |
| `[]`           | `list` |
| `"texte"`      | `str`  |
| `123`          | `int`  |
| `true / false` | `bool` |

---

## 🔹 Lecture d’un fichier JSON en Python

```python
import json

with open("config.json", "r", encoding="utf-8") as f:
    config = json.load(f)
```

➡️ Le contenu JSON devient un **dictionnaire Python**.

---

## 🔹 Récupération simple des données

```python
organisme = config["Organisme"]
base = config["BaseDeDonnees"]
photos = config["DossierPhotos"]

print(organisme)
```

Affichage :

```
lycee
```

---

## 🔹 Accès sécurisé (recommandé)

```python
organisme = config.get("Organisme", "inconnu")
```

---

## 🔹 Bonnes pratiques

- JSON sert à **configurer**, pas à stocker des données massives
- Pas de commentaires dans un fichier JSON
- Toujours utiliser l’encodage UTF-8
- Indenter pour la lisibilité (`indent=4`)
---

## 🔹 Écriture d’un fichier JSON en Python

```python
import json

config = {
    "Organisme": "lycee",
    "BaseDeDonnees": "eleves.db",
    "DossierPhotos": "photos/lycee"
}

with open("config.json", "w", encoding="utf-8") as f:
    json.dump(config, f, indent=4, ensure_ascii=False)
```

- indent=4 : ajoute une indentation pour rendre le fichier JSON lisible (espaces entre les niveaux)
- ensure_ascii=False : permet de conserver les caractères accentués

---

## 🔹 À retenir

> Charger ou écrire un fichier JSON en Python revient à manipuler un dictionnaire.

JSON est idéal pour **adapter le comportement d’un programme sans modifier le code**.
